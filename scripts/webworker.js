/*const date = new Date();

function clockRefresh() {
	let hours = date.getHours();
	let minutes = date.getMinutes();
	
	console.log("test");

		/*Tetenary Operator, Don't use these
		 * i'm just testing
	
		**/ 
	/*
	let ampm = (hours >= 12) ? "PM" : "AM";
	postMessage(hours +":"+minutes+" "+ampm);
	setTimeout("clockRefresh()", 1500);
}
